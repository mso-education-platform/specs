{
  "name": "specs-root",
  "private": true,
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "precommit:check": "npm --prefix app run prisma:generate && npm --prefix app run lint && npm --prefix app run build && npm --prefix app run test"
  }
}
